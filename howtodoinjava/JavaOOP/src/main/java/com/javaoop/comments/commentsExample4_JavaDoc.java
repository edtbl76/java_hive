package com.javaoop.comments;

public class commentsExample4_JavaDoc {

    /**
     * The documentation comments are used when you want to expose information to be parsed by the 'javadoc'
     * utility.
     *
     * This is the information that you see in editors when using the AUTOCOMPLETE feature. These comments are put
     * above classes, interfaces and method definitions.
     * Doc comments always begin with "/**" and end with "* /" (no space)
     * @param args not doing anything
     * @returns nothing at all.
     */
    public static void main(String[] args) {
        System.exit(0);
    }
}
